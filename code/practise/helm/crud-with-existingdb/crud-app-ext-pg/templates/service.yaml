apiVersion: v1
kind: Service
metadata:
  name: {{ include "crud-app-ext-pg.fullname" . }}
  labels:
    {{- include "crud-app-ext-pg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crud-app-ext-pg.selectorLabels" . | nindent 4 }}
