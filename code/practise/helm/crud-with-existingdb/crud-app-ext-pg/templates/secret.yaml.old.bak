# apiVersion: v1
# kind: Secret
# metadata:
  # name: {{ include "crud-app-ext-pg.fullname" . }}-secret
# type: Opaque
# data:
  # database: {{ .Values.postgres.database | b64enc }}
  #username: {{ .Values.postgres.username | b64enc }}
  #password: {{ .Values.postgres.password | b64enc }} we have removed from values.yaml file so removing the referencing and we have direclty provided in 
  #helm/crud-with-existingdb/crud-app-ext-pg/templates/tls-and-db-secret.yaml file so we dont need to hardcode the values file


# we will use crud-app-ext-pg/templates/tls-and-db-secret.yaml file now as we are changing to  https with custom certs and also password will be updated and refered from this file. 
# in deployment file we are refereing usr and pass from tls-and-db-secret.yaml secrets and rest directly from values.yaml like host, port and db name so commenting out and renaming the extension as helm will read all the .yaml files


# ┌──(ashfaq㉿kali-vm)-[~/…/code/k8s/helm/crud-with-existingdb]
# └─$ helm lint  ./crud-app-ext-pg
# ==> Linting ./crud-app-ext-pg
# [INFO] Chart.yaml: icon is recommended

# 1 chart(s) linted, 0 chart(s) failed

# post renaming no linting errors