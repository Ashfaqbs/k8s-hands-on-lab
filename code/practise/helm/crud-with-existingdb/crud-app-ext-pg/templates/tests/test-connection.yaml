apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crud-app-ext-pg.fullname" . }}-test-connection"
  labels:
    {{- include "crud-app-ext-pg.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crud-app-ext-pg.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
