apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "simplerestapi.fullname" . }}-test-connection"
  labels:
    {{- include "simplerestapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "simplerestapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
